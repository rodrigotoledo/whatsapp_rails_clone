default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  primary:
    <<: *default
    database: whatsapp_clone_development.sqlite3
  queue:
    <<: *default
    database: storage/whatsapp_clone_queue.sqlite3
    migrations_paths: db/queue_migrate
  cache:
    <<: *default
    database: storage/whatsapp_clone_development_cache.sqlite3
    migrations_path: db/cache_migrate
  cable:
    <<: *default
    database: storage/whatsapp_clone_development_cable.sqlite3
    migrations_path: db/cable_migrate

test:
  <<: *default
  database: storage/whatsapp_clone_test.sqlite3

production:
  <<: *default
  database: storage/whatsapp_clone_production.sqlite3
  cache:
    <<: *default
    database: production_cache.sqlite3
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: storage/production_queue.sqlite3
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: storage/production_cable.sqlite3
    migrations_paths: db/cable_migrate
